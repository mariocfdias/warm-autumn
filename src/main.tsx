import React from "react";
import ReactDOM from "react-dom/client";
import { WarmAutumn } from "./components/WarmAutumn";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <WarmAutumn />
  </React.StrictMode>
);
